name: Continuous Integration

on:
  pull_request:
    branches: ['main', 'develop']
  workflow_call:

jobs:
  build-and-test:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Restore dependencies
        run: dotnet restore WasteReporting.sln

      - name: Build
        run: dotnet build WasteReporting.sln --no-restore --configuration Release

      - name: Run Unit Tests
        run: dotnet test src/WasteReporting.Tests/WasteReporting.Tests.csproj --no-build --configuration Release --verbosity normal

      - name: Run BDD Automated Tests
        run: docker-compose -f docker-compose.test.yml up --build --abort-on-container-exit --exit-code-from bdd-tests

      - name: Publish API
        run: dotnet publish src/WasteReporting.API/WasteReporting.API.csproj -c Release -o ./publish

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: api-artifact
          path: ./publish
